"use strict";(self.webpackChunkhandbook=self.webpackChunkhandbook||[]).push([[3716],{6467:(e,n,a)=>{a.r(n),a.d(n,{assets:()=>d,contentTitle:()=>r,default:()=>c,frontMatter:()=>o,metadata:()=>s,toc:()=>l});var t=a(5893),i=a(1151);const o={title:"Tutorial: Embed a node in Tauri"},r=void 0,s={id:"tutorials/tauri",title:"Tutorial: Embed a node in Tauri",description:"In this tutorial we will see how to embed an aquadoggo node in a Tauri app. Tauri is a framework for building apps which have a Rust backend and WebView frontend which will compile for all major desktop platforms (mobile coming soon). This means we can easily package our aquadoggo node alongside a frontend written in your favorite web framework (or none at all!).",source:"@site/docs/tutorials/tauri.md",sourceDirName:"tutorials",slug:"/tutorials/tauri",permalink:"/tutorials/tauri",draft:!1,unlisted:!1,tags:[],version:"current",frontMatter:{title:"Tutorial: Embed a node in Tauri"}},d={},l=[{value:"What do I need?",id:"what-do-i-need",level:2},{value:"Download the application code",id:"download-the-application-code",level:2},{value:"Run the application",id:"run-the-application",level:2},{value:"How does it all work?",id:"how-does-it-all-work",level:2},{value:"Embedding an <code>aquadoggo</code> node",id:"embedding-an-aquadoggo-node",level:2},{value:"Add <code>aquadoggo</code> to <code>Cargo.toml</code>",id:"add-aquadoggo-to-cargotoml",level:3},{value:"Start the node",id:"start-the-node",level:3},{value:"Tauri integration",id:"tauri-integration",level:2},{value:"Project structure",id:"project-structure",level:3},{value:"<code>build.rs</code>",id:"buildrs",level:4},{value:"<code>icons</code>",id:"icons",level:4},{value:"<code>resources/</code>",id:"resources",level:4},{value:"<code>Tauri.toml</code>",id:"tauritoml",level:4},{value:"Build the Tauri app",id:"build-the-tauri-app",level:4},{value:"Launching <code>aquadoggo</code>",id:"launching-aquadoggo",level:3},{value:"Access the logs",id:"access-the-logs",level:3},{value:"The <code>WebView</code>",id:"the-webview",level:3},{value:"Persistent storage",id:"persistent-storage",level:3},{value:"Node configuration",id:"node-configuration",level:3},{value:"Schema migration",id:"schema-migration",level:3},{value:"Tauri commands",id:"tauri-commands",level:3},{value:"Temp app storage during development",id:"temp-app-storage-during-development",level:3},{value:"Multiple nodes / replication",id:"multiple-nodes--replication",level:3},{value:"Multi-platform compilation",id:"multi-platform-compilation",level:3},{value:"Local compilation",id:"local-compilation",level:4},{value:"<code>ci</code> bundle release",id:"ci-bundle-release",level:4}];function h(e){const n={a:"a",admonition:"admonition",code:"code",em:"em",h2:"h2",h3:"h3",h4:"h4",img:"img",li:"li",mdxAdmonitionTitle:"mdxAdmonitionTitle",p:"p",pre:"pre",ul:"ul",...(0,i.a)(),...e.components},{Details:o}=n;return o||function(e,n){throw new Error("Expected "+(n?"component":"object")+" `"+e+"` to be defined: you likely forgot to import, pass, or provide it.")}("Details",!0),(0,t.jsxs)(t.Fragment,{children:[(0,t.jsxs)(n.p,{children:["In this tutorial we will see how to embed an ",(0,t.jsx)(n.code,{children:"aquadoggo"})," node in a Tauri app. Tauri is a framework for building apps which have a Rust backend and WebView frontend which will compile for all major desktop platforms (mobile coming soon). This means we can easily package our ",(0,t.jsx)(n.code,{children:"aquadoggo"})," node alongside a frontend written in your favorite web framework (or none at all!)."]}),"\n",(0,t.jsx)(n.p,{children:"In this tutorial we will focus on the backend code, where we'll embed our node, implement secure persistent storage for both WebView (LocalStorage) and node data, demonstrate useful node configuration flows, populate the node with app schemas on startup and define commands callable from the frontend."}),"\n",(0,t.jsx)(n.p,{children:"We'll look at the frontend code which demonstrates advanced GraphQL queries and how to publish and retrieve blobs in the following tutorials."}),"\n",(0,t.jsx)(n.h2,{id:"what-do-i-need",children:"What do I need?"}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"NodeJS"}),"\n",(0,t.jsx)(n.li,{children:"Rust"}),"\n",(0,t.jsx)(n.li,{children:"Tauri dependencies"}),"\n",(0,t.jsx)(n.li,{children:"Editor"}),"\n",(0,t.jsx)(n.li,{children:"Terminal"}),"\n"]}),"\n",(0,t.jsx)(n.admonition,{title:"Never worked with Rust and/or Tauri before?",type:"info",children:(0,t.jsx)(n.p,{children:"This tutorial requires you to have a working Rust environment and all Tauri dependencies installed. If you have never worked with Rust and/or Tauri before this is no problem! Setting it up is fairly easy and besides using some basic command line commands there is no more Rust knowledge required to make the node run on your computer."})}),"\n",(0,t.jsxs)(o,{children:[(0,t.jsx)("summary",{children:"How do I install NodeJS?"}),(0,t.jsx)("div",{children:(0,t.jsxs)(n.p,{children:["You can check out the official ",(0,t.jsx)(n.a,{href:"https://nodejs.org/en/download/package-manager/",children:"Installing Node.js via package manager"})," guidelines here. But we would recommend you installing a NodeJS version manager like ",(0,t.jsx)(n.a,{href:"https://github.com/nvm-sh/nvm",children:"nvm"}),", or even better ",(0,t.jsx)(n.a,{href:"https://github.com/tj/n",children:"n"}),". We used the NodeJS version ",(0,t.jsx)(n.code,{children:"18.17.0"})," for this tutorial."]})})]}),"\n",(0,t.jsxs)(o,{children:[(0,t.jsx)("summary",{children:"How do I install Rust & Tauri dependencies?"}),(0,t.jsxs)("div",{children:[(0,t.jsxs)(n.p,{children:["Make sure you have a working Rust environment and all Tauri dependencies installed on your computer before you begin with the tutorial. You can check Rust is installed by running ",(0,t.jsx)(n.code,{children:"rustc --version"})," in your terminal. This tutorial was written with Rust version ",(0,t.jsx)(n.code,{children:"1.75.0"})," but it will probably also work with other versions."]}),(0,t.jsxs)(n.p,{children:["If you don't have Rust installed yet or you need just the Tauri dependencies you can follow the steps from the Tauri website: ",(0,t.jsx)(n.a,{href:"https://tauri.app/v1/guides/getting-started/prerequisites",children:"Tauri: Getting Started"}),"."]})]})]}),"\n",(0,t.jsxs)(n.admonition,{type:"info",children:[(0,t.jsxs)(n.mdxAdmonitionTitle,{children:["What is ",(0,t.jsx)(n.code,{children:"aquadoggo"}),"?"]}),(0,t.jsxs)(n.p,{children:["If you aren't familiar with ",(0,t.jsx)(n.code,{children:"aquadoggo"})," then I recommend checking out the ",(0,t.jsx)(n.a,{href:"/tutorials/aquadoggo",children:'"Set up a local node"'})," tutorial before you do this one. It focuses on using ",(0,t.jsx)(n.code,{children:"aquadoggo"})," on the command line, but all the features and configuration parameters are the same when working with it programmatically."]})]}),"\n",(0,t.jsx)(n.h2,{id:"download-the-application-code",children:"Download the application code"}),"\n",(0,t.jsxs)(n.p,{children:["Everything we need for this tutorial is in the ",(0,t.jsx)(n.code,{children:"p2panda/tauri-example"})," repository. Download the code and install NodeJS dependencies like so."]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:"# Clone the `tauri-example` git repository\ngit clone https://github.com/p2panda/tauri-example\n\n# Move into the folder you've just created\ncd tauri-example\n\n# Install NodeJS dependencies\nnpm install\n"})}),"\n",(0,t.jsx)(n.h2,{id:"run-the-application",children:"Run the application"}),"\n",(0,t.jsx)(n.p,{children:"First we're going to simply run the application, this will compile the front and backend and launch the app in dev mode. The first step (compilation) will likely take... a while.... don't worry though, this is only the case the first time you run the command, after that only incremental compilation is required and things will be a lot quicker."}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:"# Run the tauri app in dev mode\nnpm run tauri dev\n"})}),"\n",(0,t.jsx)(n.admonition,{title:'What\'s "dev" mode?',type:"info",children:(0,t.jsx)(n.p,{children:'When we launch an app in "dev" mode it means we are still in development, and so rather than an optimized build with production settings, we want to work in a developer friendly environment. In this case that means we don\'t persist any app data to the filesystem and want code hot-reloading to be activated (the app updates automatically when we edit the code).'})}),"\n",(0,t.jsx)(n.p,{children:"You should see some output in your terminal like this (missing out the long compilation process):"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:"npm run tauri dev\n\n> p2panda-tauri-example@0.0.0 tauri\n> tauri dev\n\n     Running BeforeDevCommand (`npm run dev`)\n\n> p2panda-tauri-example@0.0.0 dev\n> vite\n\n\n  VITE v4.5.2  ready in 135 ms\n\n  \u279c  Local:   http://localhost:1420/\n  \u279c  Network: use --host to expose\n...\n...\n...\n        Info Watching /home/sandreae/Code/tauri-example-test/src-tauri for changes...\n    Finished dev [unoptimized + debuginfo] target(s) in 0.19s\nGo to http://0.0.0.0:2020/graphql to use GraphQL playground\nPeer id: 12D3KooWPzHvt52CYkYPvejsBjgjcsqyBszcEQrDhDp6RSdYx6qx\nNode is listening on 0.0.0.0:2022\nSchema migration: app schemas successfully deployed on initial start-up\n"})}),"\n",(0,t.jsxs)(n.p,{children:["We're using ",(0,t.jsx)(n.code,{children:"vite"})," to package the frontend code and assets, you can see that a dev server is started at ",(0,t.jsx)(n.code,{children:"http://localhost:1420/"}),". This is where Tauri will look for the frontend app during development. Next the Rust code is compiled and eventually the app is launched. The last four lines are logging from the ",(0,t.jsx)(n.code,{children:"aquadoggo"})," node which has now started up. You can go to ",(0,t.jsx)(n.code,{children:"localhost:2020/graphql"})," to check the GraphQL playground is being served correctly."]}),"\n",(0,t.jsx)(n.p,{children:"The app window should now have opened, it'll be a blank screen.... not very interesting... until you start clicking! Then you can draw pointless messages like this with panda gif stickers (yay!):"}),"\n",(0,t.jsx)(n.p,{children:(0,t.jsx)(n.img,{src:a(3348).Z+"",width:"828",height:"666"})}),"\n",(0,t.jsx)(n.h2,{id:"how-does-it-all-work",children:"How does it all work?"}),"\n",(0,t.jsxs)(n.p,{children:["A ",(0,t.jsx)(n.em,{children:"lot"})," happened there, but it's worth pointing out now that you don't actually need to know much about it if you just want to start hacking on a web client (for that you can go straight to ",(0,t.jsx)(n.code,{children:"src/"})," where all the frontend JavaScript code is). This tutorial would be quite short if we didn't dig a bit deeper though. Let's take it step by step."]}),"\n",(0,t.jsxs)(n.p,{children:["As we saw from the logging, a node ",(0,t.jsx)(n.em,{children:"did"})," start up, so where did it come from?"]}),"\n",(0,t.jsxs)(n.h2,{id:"embedding-an-aquadoggo-node",children:["Embedding an ",(0,t.jsx)(n.code,{children:"aquadoggo"})," node"]}),"\n",(0,t.jsxs)(n.h3,{id:"add-aquadoggo-to-cargotoml",children:["Add ",(0,t.jsx)(n.code,{children:"aquadoggo"})," to ",(0,t.jsx)(n.code,{children:"Cargo.toml"})]}),"\n",(0,t.jsxs)(n.p,{children:["The absolute first thing we need to do to use ",(0,t.jsx)(n.code,{children:"aquadoggo"})," in any Rust project is to add it as a dependency in our ",(0,t.jsx)(n.code,{children:"Cargo.toml"})," file. We'll need ",(0,t.jsx)(n.code,{children:"p2panda-rs"})," for creating an identity for the node too so let's add that now as well."]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-toml",children:'[dependencies]\np2panda-rs = "0.8.1"\naquadoggo = "0.7.1"\n'})}),"\n",(0,t.jsxs)(n.p,{children:["The ",(0,t.jsx)(n.code,{children:"aquadoggo"})," crate gives us access to an API with which we can configure, start, migrate data to, and stop our node."]}),"\n",(0,t.jsx)(n.admonition,{title:"What about publishing and querying data?",type:"info",children:(0,t.jsxs)(n.p,{children:["This is all done from the frontend code via the ",(0,t.jsx)(n.code,{children:"GraphQL"})," API!"]})}),"\n",(0,t.jsx)(n.h3,{id:"start-the-node",children:"Start the node"}),"\n",(0,t.jsx)(n.p,{children:"We can start a node like this:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:"use aquadoggo::{Configuration, Node};\nuse p2panda_rs::identity::KeyPair;\n\n// We're using `tokio::runtime` to make main async.\n#[tokio::main]\nasync fn main() {\n    // Generate a key pair for the node identity.\n    let key_pair = KeyPair::new();\n\n    // Construct a default configuration.\n    let config = Configuration::default();\n\n    // Start the node!\n    let node = Node::start(key_pair, config).await;\n\n    // The node is already started now, wait here on the blocking future `on_exit` which resolves when any of the node's services exit with an\n    // error or because of user closure.\n    node.on_exit().await;\n\n    // Finally we call `shutdown` to gracefully close all remaining services, including database connections.\n    node.shutdown().await;\n}\n"})}),"\n",(0,t.jsx)(n.p,{children:"You can see the complete runtime life of a node in the above code, mostly it spends it's time running in the background, responding to HTTP and GraphQL requests from clients, and replicating with other nodes. The main point of interest for this tutorial is how we wrap this simple code into a Tauri app and configure the node at runtime."}),"\n",(0,t.jsx)(n.h2,{id:"tauri-integration",children:"Tauri integration"}),"\n",(0,t.jsx)(n.h3,{id:"project-structure",children:"Project structure"}),"\n",(0,t.jsxs)(n.p,{children:["Let's take a look into the project directory ",(0,t.jsx)(n.code,{children:"src-tauri/"}),":"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{children:"\u251c\u2500\u2500 build.rs\n\u251c\u2500\u2500 Cargo.lock\n\u251c\u2500\u2500 Cargo.toml\n\u251c\u2500\u2500 icons\n\u2502\xa0\xa0 \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 resources\n\u2502\xa0\xa0 \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 src\n\u2502\xa0\xa0 \u2502\u2500\u2500 main.rs\n\u2502\xa0\xa0 \u2514\u2500\u2500 ...\n\u2514\u2500\u2500 Tauri.toml\n"})}),"\n",(0,t.jsxs)(n.p,{children:["To anyone who's worked with the Rust programming language much of this will look very familiar. We have a ",(0,t.jsx)(n.code,{children:"Cargo.toml"})," and ",(0,t.jsx)(n.code,{children:"Cargo.lock"})," for managing dependencies, and a ",(0,t.jsx)(n.code,{children:"src/"})," folder where our project code lives. Everything else here are Tauri specific conventions."]}),"\n",(0,t.jsxs)(n.p,{children:["If you check the ",(0,t.jsx)(n.code,{children:"Cargo.toml"})," file you'll see that we're importing the ",(0,t.jsx)(n.code,{children:"tauri"})," crate as a dependency:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-toml",children:'[dependencies]\ntauri = { version = "1.4", features = ["shell-open", "config-toml"] }\n'})}),"\n",(0,t.jsx)(n.admonition,{title:"Tauri has much more",type:"info",children:(0,t.jsxs)(n.p,{children:["There are many useful features of Tauri that we won't touch on at all in this tutorial, checkout out the ",(0,t.jsx)(n.a,{href:"https://tauri.app/v1/guides/features/",children:'"features page"'})," for more info."]})}),"\n",(0,t.jsx)(n.p,{children:"And here is a brief explanation of the other files and folders used by the Tauri app:"}),"\n",(0,t.jsx)(n.h4,{id:"buildrs",children:(0,t.jsx)(n.code,{children:"build.rs"})}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"the default Tauri build file, we won't do anything here in this tutorial."}),"\n"]}),"\n",(0,t.jsx)(n.h4,{id:"icons",children:(0,t.jsx)(n.code,{children:"icons"})}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsxs)(n.li,{children:["icons for your app in many platform specific shapes and sizes. We won't do anything here in this tutorial. Check the Tauri docs on how to ",(0,t.jsx)(n.a,{href:"https://tauri.app/v1/api/cli#icon",children:"generate your own icons"}),"."]}),"\n"]}),"\n",(0,t.jsx)(n.h4,{id:"resources",children:(0,t.jsx)(n.code,{children:"resources/"})}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"resources we want access to at application runtime can be placed here and retrieved via the Tauri API."}),"\n"]}),"\n",(0,t.jsx)(n.h4,{id:"tauritoml",children:(0,t.jsx)(n.code,{children:"Tauri.toml"})}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsx)(n.li,{children:"this is our Tauri app configuration file, there are many features with fine grained configurable options."}),"\n"]}),"\n",(0,t.jsx)(n.admonition,{title:"Where did all this come from?",type:"info",children:(0,t.jsxs)(n.p,{children:["You can scaffold a Tauri project using their ",(0,t.jsx)(n.code,{children:"create-tauri-app"})," tool which is available via ",(0,t.jsx)(n.code,{children:"cargo"}),", ",(0,t.jsx)(n.code,{children:"npm"})," and more. This very project was scaffolded in that way. More information on their ",(0,t.jsx)(n.a,{href:"https://tauri.app/v1/guides/getting-started/setup/",children:'"Quick Start"'})," page."]})}),"\n",(0,t.jsx)(n.p,{children:"Ok, let's get into some code now, we'll step through building a Tauri app, starting our node and getting access to useful logs."}),"\n",(0,t.jsx)(n.h4,{id:"build-the-tauri-app",children:"Build the Tauri app"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:'// src/main.rs\nfn main() {\n    tauri::Builder::default()\n        .run(tauri::generate_context!())\n        .expect("error while running tauri application");\n}\n'})}),"\n",(0,t.jsxs)(n.p,{children:["This is the Tauri builder, the code above builds an app which... doesn't do anything... let's already add our ",(0,t.jsx)(n.code,{children:"aquadoggo"}),"."]}),"\n",(0,t.jsxs)(n.h3,{id:"launching-aquadoggo",children:["Launching ",(0,t.jsx)(n.code,{children:"aquadoggo"})]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:'// src/main.rs\n// highlight-start\nuse aquadoggo::{Configuration, Node};\nuse p2panda_rs::identity::KeyPair;\n\n/// Setup handler where we launch our `aquadoggo` before the app starts.\nfn setup_handler(app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error + \'static>> {\n    let key_pair = KeyPair::new();\n    let config = Configuration::default();\n\n    tauri::async_runtime::spawn(async {\n        let node = Node::start(key_pair, config).await;\n        node.on_exit().await;\n        node.shutdown().await;\n    });\n\n    Ok(())\n}\n// highlight-end\n\nfn main() {\n    tauri::Builder::default()\n        // highlight-start\n        // Add setup handler which launches our node before the app runs.\n        .setup(setup_handler)\n        // highlight-end\n        .run(tauri::generate_context!())\n        .expect("error while running tauri application");\n}\n'})}),"\n",(0,t.jsx)(n.p,{children:"This will look very familiar to the example code for starting a node that we looked at earlier. The only change is that we are launching the node in it's own async task from a setup handler which the Tauri app builder runs before the application starts. We will be doing more setup here in later steps."}),"\n",(0,t.jsx)(n.h3,{id:"access-the-logs",children:"Access the logs"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:'// src/main.rs\nuse aquadoggo::{Configuration, Node};\nuse p2panda_rs::identity::KeyPair;\n\n/// Setup handler where we launch our `aquadoggo` before the app starts.\nfn setup_handler(app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error + \'static>> {\n    let key_pair = KeyPair::new();\n    let config = Configuration::default();\n\n    tauri::async_runtime::spawn(async {\n        let node = Node::start(key_pair, config).await;\n        node.on_exit().await;\n        node.shutdown().await;\n    });\n\n    Ok(())\n}\n\nfn main() {\n    // highlight-start\n    // Enable logging if set via `RUST_LOG` environment variable.\n    if std::env::var("RUST_LOG").is_ok() {\n        let _ = env_logger::builder().try_init();\n    }\n    // highlight-end\n\n    tauri::Builder::default()\n        .setup(setup_handler)\n        .run(tauri::generate_context!())\n        .expect("error while running tauri application");\n}\n'})}),"\n",(0,t.jsxs)(n.p,{children:["We will want access to logs from our application and any important dependencies (especially from ",(0,t.jsx)(n.code,{children:"aquadoggo"}),") to do that we're building a ",(0,t.jsx)(n.code,{children:"Logger"})," instance which will capture and make available any logs bubbling up."]}),"\n",(0,t.jsxs)(n.p,{children:["Now if we start the app, ",(0,t.jsx)(n.code,{children:"aquadoggo"})," should launch in the background. We can access the logs to check this is happening correctly:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:"npm run tauri dev\n...\n...\n...\n    Finished dev [unoptimized + debuginfo] target(s) in 42.59s\nGo to http://0.0.0.0:2020/graphql to use GraphQL playground\nPeer id: 12D3KooWFNVNAaR16ciUise7W2mMmodD993BDANE44q3NJehXTuz\nNode is listening on 0.0.0.0:2022```\n[2024-01-26T10:36:31Z INFO  aquadoggo::manager] Start materializer service\n[2024-01-26T10:36:31Z INFO  aquadoggo::materializer::worker] Register reduce worker with pool size 16\n[2024-01-26T10:36:31Z INFO  aquadoggo::materializer::worker] Register dependency worker with pool size 16\n[2024-01-26T10:36:31Z INFO  aquadoggo::materializer::worker] Register schema worker with pool size 16\n[2024-01-26T10:36:31Z INFO  aquadoggo::materializer::worker] Register blob worker with pool size 16\n[2024-01-26T10:36:31Z INFO  aquadoggo::materializer::worker] Register garbage_collection worker with pool size 16\n[2024-01-26T10:36:31Z INFO  aquadoggo::manager] Start http service\n[2024-01-26T10:36:31Z INFO  aquadoggo::manager] Start network service\n[2024-01-26T10:36:31Z INFO  aquadoggo::network::service] Networking service initializing...\n[2024-01-26T10:36:31Z INFO  aquadoggo::network::service] Network service ready!\n[2024-01-26T10:36:31Z INFO  aquadoggo::manager] Start replication service\n"})}),"\n",(0,t.jsx)(n.p,{children:"Ok, that's great! We're already quite far. Next up is to start integrating some core Tauri features."}),"\n",(0,t.jsxs)(n.h3,{id:"the-webview",children:["The ",(0,t.jsx)(n.code,{children:"WebView"})]}),"\n",(0,t.jsx)(n.p,{children:"Let's add a WebView to our app, this is where the client code and UI will be running:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:'use aquadoggo::{Configuration, Node};\nuse p2panda_rs::identity::KeyPair;\n\n/// Setup handler where we launch our `aquadoggo` before the app starts.\nfn setup_handler(app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error + \'static>> {\n    // highlight-start\n    // Get a handle on the running application which gives us access to global state and\n    // app configuration values.\n    let app = app.handle();\n\n    // Build the WebView.\n    tauri::WindowBuilder::new(&app, "main", tauri::WindowUrl::App("index.html".into()))\n        .resizable(false)\n        .fullscreen(false)\n        .inner_size(800.0, 600.0)\n        .title("p2panda-tauri-example")\n        .build()?;\n    // highlight-end\n\n    let key_pair = KeyPair::new();\n    let config = Configuration::default();\n\n    tauri::async_runtime::spawn(async {\n        let node = Node::start(key_pair, config).await;\n        node.on_exit().await;\n        node.shutdown().await;\n    });\n\n    Ok(())\n}\n'})}),"\n",(0,t.jsx)(n.h3,{id:"persistent-storage",children:"Persistent storage"}),"\n",(0,t.jsxs)(n.p,{children:["There are actually two places from which application data will be persisted: one is the backend where we are working now, this will be things like the ",(0,t.jsx)(n.code,{children:"aquadoggo"})," identity, config and database, the other is from the frontend client code. When working in the browser we're quite used to using LocalStorage to persist data between sessions. We use this very same pattern in Tauri, except we now have control over where and how this data is stored on the filesystem."]}),"\n",(0,t.jsxs)(n.p,{children:["On a Linux platform the app data directory will be ",(0,t.jsx)(n.code,{children:"~/.local/share/{identifier}"})," with ",(0,t.jsx)(n.code,{children:"{identifier}"})," coming from your ",(0,t.jsx)(n.code,{children:"Tauri.toml"})," configuration file:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-toml",children:'# Tauri.toml\n[tauri.bundle]\nidentifier = "p2panda-tauri-example"\n'})}),"\n",(0,t.jsx)(n.p,{children:"For our application we want both our WebView and node data to be persisted between app runs, and it would be nice if it was in the same location, so let's configure it that way now."}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:'// highlight-start\n/// Utilities for key pairs.\nmod key_pair;\n// highlight-end\n\nuse aquadoggo::{Configuration, Node};\nuse p2panda_rs::identity::KeyPair;\n\n// highlight-start\nuse crate::key_pair::generate_or_load_key_pair;\n// highlight-end\n\n/// Setup handler where we launch our `aquadoggo` before the app starts.\nfn setup_handler(app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error + \'static>> {\n    // Get a handle on the running application which gives us access to global state and\n    // app configuration values.\n    let app = app.handle();\n\n    // highlight-start\n    // Get the recommended app data directory.\n    let app_data_dir = app\n        .path_resolver()\n        .app_data_dir()\n        .expect("recommended app data dir is detected");\n\n    // If it doesn\'t exist yet, create it now.\n    if !app_data_dir.is_dir() {\n        DirBuilder::new().create(app_data_dir.clone())?;\n    };\n    // highlight-end\n\n    // Build the WebView.\n    tauri::WindowBuilder::new(&app, "main", tauri::WindowUrl::App("index.html".into()))\n        // highlight-start\n        // Set the data directory for our WebView\n        .data_directory(app_data_dir.clone())\n        // highlight-end\n        .resizable(false)\n        .fullscreen(false)\n        .inner_size(800.0, 600.0)\n        .title("p2panda-tauri-example")\n        .build()?;\n\n    // highlight-start\n    // Create a KeyPair or load it from private-key.txt file in app data directory.\n    //\n    // This key pair is used to identify the node on the network, it is not used for signing\n    // any application data.\n    let key_pair = generate_or_load_key_pair(&app_data_dir)?;\n\n    let config = Configuration::default();\n    // Set database url based on app data dir path.\n    config.database_url = format!(\n        "sqlite:{}/db.sqlite3",\n        app_data_dir.to_str().expect("invalid character in path")\n    );\n    // Set blobs base path to app data dir.\n    config.blobs_base_path = Some(app_data_dir);\n    // highlight-end\n\n    tauri::async_runtime::spawn(async {\n        let node = Node::start(key_pair, config).await;\n        node.on_exit().await;\n        node.shutdown().await;\n    });\n\n    Ok(())\n}\n'})}),"\n",(0,t.jsxs)(n.p,{children:["There's a bit more going on in ",(0,t.jsx)(n.code,{children:"setup_handler"})," now. First we get a handle on the running app from which we can access global state and configuration values. From this we get the recommended app data directory path and create it if it doesn't exist yet (it won't if this is the first time the app is run). We then set the data directory for our ",(0,t.jsx)(n.code,{children:"WebView"}),", load or generate a new keypair, and configure node storage locations for our database and blobs."]}),"\n",(0,t.jsxs)(n.p,{children:["We're using a new crate module ",(0,t.jsx)(n.code,{children:"key_pair"})," for the logic around loading or generating our node identity, we won't look into that now as it's not so interesting for this tutorial, but feel free to head over to ",(0,t.jsx)(n.code,{children:"src/key_pair.rs"})," if you want to know more."]}),"\n",(0,t.jsx)(n.h3,{id:"node-configuration",children:"Node configuration"}),"\n",(0,t.jsxs)(n.p,{children:["Now we have a running node which will persist app data between sessions, great! If you've checked out the ",(0,t.jsx)(n.a,{href:"/tutorials/aquadoggo",children:'"Set up a local node"'})," tutorial then you'll know there are many ",(0,t.jsx)(n.code,{children:"aquadoggo"})," configurations to play with, and we will likely want users of our app to be able to decide some of these themselves."]}),"\n",(0,t.jsxs)(n.p,{children:["The ",(0,t.jsx)(n.code,{children:"aquadoggo"})," CLI can be configured via a ",(0,t.jsx)(n.a,{href:"https://github.com/p2panda/aquadoggo/blob/main/aquadoggo_cli/config.toml",children:(0,t.jsx)(n.code,{children:"config.toml"})})," file and we're going to use the same approach in our Tauri app. We'll take the following steps to do this:"]}),"\n",(0,t.jsxs)(n.ul,{children:["\n",(0,t.jsxs)(n.li,{children:["include a default ",(0,t.jsx)(n.code,{children:"config.toml"})," in our apps ",(0,t.jsx)(n.code,{children:"resources"})," and copy it into the app data directory on first start-up"]}),"\n",(0,t.jsxs)(n.li,{children:["load this file and configure our ",(0,t.jsx)(n.code,{children:"aquadoggo"})," node with it when the app runs"]}),"\n"]}),"\n",(0,t.jsxs)(n.p,{children:["To do this we make use of the Tauri ",(0,t.jsx)(n.code,{children:"resources"})," directory. We can see that this is configured in our ",(0,t.jsx)(n.code,{children:"Tauri.toml"})," file like this:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-toml",children:'# Tauri.toml\nresources = [ "resources/*" ]\n'})}),"\n",(0,t.jsxs)(n.p,{children:["Doing this means that any files in the ",(0,t.jsx)(n.code,{children:"resources"})," directory will be bundled together with our app during compilation and we can access them via the ",(0,t.jsx)(n.a,{href:"https://docs.rs/tauri/1.5.4/tauri/struct.PathResolver.html#method.resolve_resource",children:(0,t.jsx)(n.code,{children:"resolve_resource"})})," method we get from the ",(0,t.jsx)(n.code,{children:"tauri"})," crate's ",(0,t.jsx)(n.code,{children:"PathResolver"}),"."]}),"\n",(0,t.jsxs)(n.p,{children:["At this point we will also perform a little refactor as our app setup code is growing. We'll introduce a new module ",(0,t.jsx)(n.code,{children:"config"})," and export a method for handling all of the above. We won't look into this module code in detail as it is fairly generic, see ",(0,t.jsx)(n.code,{children:"src/config.rs"})," if you want to know more. We also add our default ",(0,t.jsx)(n.code,{children:"config.toml"})," file to ",(0,t.jsx)(n.code,{children:"resources/config.toml"}),". Our setup code now looks like this:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:'mod key_pair;\n// highlight-start\n/// Utilities for node configuration.\nmod config;\n// highlight-end\n\nuse aquadoggo::Node;\nuse p2panda_rs::identity::KeyPair;\n\n// highlight-start\nuse crate::config::{app_data_dir, load_config};\n// highlight-end\nuse crate::key_pair::generate_or_load_key_pair;\n\n/// Setup handler where we launch our `aquadoggo` before the app starts.\nfn setup_handler(app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error + \'static>> {\n    let app = app.handle();\n    // highlight-start\n    // Getting and creating the app data directory has been refactored away here.\n    let app_data_dir = app_data_dir(&app)?;\n    // highlight-end\n\n    // Manually construct the app WebView window as we want to set a custom data directory.\n    tauri::WindowBuilder::new(&app, "main", tauri::WindowUrl::App("index.html".into()))\n        .data_directory(app_data_dir.clone())\n        .resizable(false)\n        .fullscreen(false)\n        .inner_size(800.0, 600.0)\n        .title("p2panda-tauri-example")\n        .build()?;\n\n    // Create a KeyPair or load it from private-key.txt file in app data directory.\n    //\n    // This key pair is used to identify the node on the network, it is not used for signing\n    // any application data.\n    let key_pair = generate_or_load_key_pair(&app_data_dir)?;\n\n    // highlight-start\n    // Load the config from app data directory. If this is the first time the app is\n    // being run then the default aquadoggo config file is copied into place and used.\n    //\n    // Environment variables are also parsed and will take priority over values in the config\n    // file.\n    let config = load_config(&app, &app_data_dir)?;\n    // highlight-end\n\n    tauri::async_runtime::spawn(async {\n        let node = Node::start(key_pair, config).await;\n        node.on_exit().await;\n        node.shutdown().await;\n    });\n\n    Ok(())\n}\n'})}),"\n",(0,t.jsxs)(n.p,{children:["On a Linux platform the app config file will be located at ",(0,t.jsx)(n.code,{children:"~/.local/share/{identifier}/config.toml"}),", the embedded node can now be configured by editing this file. Check out the ",(0,t.jsx)(n.a,{href:"https://github.com/p2panda/aquadoggo/tree/main/aquadoggo_cli",children:"aquadoggo cli"})," repo for details on your many options."]}),"\n",(0,t.jsx)(n.admonition,{title:"a GUI dashboard would be nice!",type:"info",children:(0,t.jsx)(n.p,{children:"It's true, you could build a very nice GUI dashboard for handling node configuration instead of manually editing a config file. We'd be excited to see anyone's attempt at this ;-p"})}),"\n",(0,t.jsx)(n.h3,{id:"schema-migration",children:"Schema migration"}),"\n",(0,t.jsxs)(n.p,{children:["Schemas are an important aspect of developing applications with p2panda and ",(0,t.jsx)(n.code,{children:"aquadoggo"}),", they are how we define the shape of data we will be using in our application, and schema ids are how nodes communicate to each other which collections of documents they are interested in. When you build a local first p2panda application you are likely going to want to bundle these schema with your app, and publish them to your local node. If you want to know how to create a schema then at this point it's worth looking through the ",(0,t.jsx)(n.a,{href:"/tutorials/fishy",children:'"Create a schema"'})," tutorial, you can continue this tutorial without doing that though. It's enough to know that we have a pre-generated ",(0,t.jsx)(n.code,{children:"schema.lock"})," file which contains our schema, and we want to bundle this with our app and publish the contents of it to our node on startup. To do this we can use the ",(0,t.jsx)(n.code,{children:"resources"})," folder and API again."]}),"\n",(0,t.jsxs)(n.p,{children:["We will add our ",(0,t.jsx)(n.code,{children:"schema.toml"})," and ",(0,t.jsx)(n.code,{children:"schema.lock"})," files to the resources ",(0,t.jsx)(n.code,{children:"resources/"})," folder."]}),"\n",(0,t.jsx)(n.admonition,{title:"Schema in the wild",type:"info",children:(0,t.jsx)(n.p,{children:"Schema exist on a p2panda network in the same way that any other documents do, they are the result of publishing operations. Because of this, it's possible to ambiently discover schema and start publishing documents following their form. We bundle them in this application here though as we already want to use the schema even if we're offline or not connected to a network yet. It is however possible to rely on your peers to send you the schema as well."})}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:'mod key_pair;\nmod config;\n// highlight-start\n/// Utilities for schema.lock file.\nmod schema;\n// highlight-end\n\nuse aquadoggo::{Configuration, Node};\nuse p2panda_rs::identity::KeyPair;\n\nuse crate::config::{app_data_dir, load_config};\nuse crate::key_pair::generate_or_load_key_pair;\n// highlight-start\nuse crate::schema::load_schema_lock;\n// highlight-end\n\n/// Setup handler where we launch our `aquadoggo` before the app starts.\nfn setup_handler(app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error + \'static>> {\n    let app = app.handle();\n    let app_data_dir = app_data_dir(&app)?;\n\n    tauri::WindowBuilder::new(&app, "main", tauri::WindowUrl::App("index.html".into()))\n        .data_directory(app_data_dir.clone())\n        .resizable(false)\n        .fullscreen(false)\n        .inner_size(800.0, 600.0)\n        .title("p2panda-tauri-example")\n        .build()?;\n\n    let key_pair = generate_or_load_key_pair(&app_data_dir)?;\n    let config = load_config(&app, &app_data_dir)?;\n\n    // highlight-start\n    // Load the schema.lock file containing our app schema which will be published to the node.\n    let schema_lock = load_schema_lock(&app)?;\n    // highlight-end\n\n    tauri::async_runtime::spawn(async {\n        let node = Node::start(key_pair, config).await;\n        // highlight-start\n        // Migrate the app schemas, returns true if schema were migrated, false if no migration was required.\n        let did_migrate_schemas = node\n            .migrate(schema_lock)\n            .await\n            .expect("failed to migrate app schema");\n\n        if did_migrate_schemas {\n            println!("Schema migration: app schemas successfully deployed on initial start-up");\n        }\n        // highlight-end\n\n        node.on_exit().await;\n        node.shutdown().await;\n    });\n\n    Ok(())\n}\n'})}),"\n",(0,t.jsxs)(o,{children:[(0,t.jsxs)("summary",{children:["Show ",(0,t.jsx)(n.code,{children:"schema.toml"})," file."]}),(0,t.jsxs)("div",{children:[(0,t.jsxs)(n.p,{children:["This is the ",(0,t.jsx)(n.code,{children:"schemas.toml"})," file used to generate our ",(0,t.jsx)(n.code,{children:"schema.lock"}),":"]}),(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-toml",children:'# resources/schema.toml\n[sprite_images]\ndescription = "An image which can be assigned to a sprite"\n\n[sprite_images.fields]\nblob = { type = "relation", schema = { id = "blob_v1" } }\ndescription = { type = "str" }\ntimestamp = { type = "int" }\n\n[sprites]\ndescription = "A sprite which can be placed on a 2 dimensional plane"\n\n[sprites.fields]\nimg = { type = "relation", schema = { name = "sprite_images" } }\npos_x = { type = "int" }\npos_y = { type = "int" }\ntimestamp = { type = "int" }\ncolour = { type = "str" }\n\n'})})]})]}),"\n",(0,t.jsxs)(n.p,{children:["Ok, we're pretty much done with the setup code! We have a Tauri app that launches an ",(0,t.jsx)(n.code,{children:"aquadoggo"})," in the background, configures it based on a ",(0,t.jsx)(n.code,{children:"config.toml"})," file, persists app data to platform default storage location, and migrates relevant schema to the node on first startup. We really are ready to build a frontend client! This however, is now up to you now :-) there is a GraphQL API waiting to serve your app, you can publish data using the p2panda TypeScript SDK ",(0,t.jsx)(n.code,{children:"shirokuma"}),", and any GraphQL client can be used for querying documents from the embedded node. Have a look at the tutorial ",(0,t.jsx)(n.a,{href:"/tutorials/mushroom-app",children:'"Let\'s build a mushroom app"'})," to learn more about client building."]}),"\n",(0,t.jsx)(n.admonition,{title:"But what about the panda stickers?!",type:"info",children:(0,t.jsxs)(n.p,{children:["There is a JavaScript client app wrapped in this project of course, otherwise none of the clicky panda sticker fun would be possible. Have a look into the ",(0,t.jsx)(n.code,{children:"src/"})," directory to explore the code. It's a very simple client, but still demonstrates the powerful query API that our node is exporting. In ",(0,t.jsx)(n.code,{children:"src/queries.js"})," you will find all the GraphQL queries the client uses, they make use of filtering, ordering and paginating document collections. Hopefully it demonstrates that thoughtful schema and query design can reduce the need for complex app logic greatly."]})}),"\n",(0,t.jsx)(n.p,{children:"This tutorial is already growing quite long! But we still have a little more exciting bonus material to finish with."}),"\n",(0,t.jsx)(n.h3,{id:"tauri-commands",children:"Tauri commands"}),"\n",(0,t.jsxs)(n.p,{children:["Tauri commands allow you to define methods in your Rust code and automatically have them exposed in the client. We will use this now to communicate the ",(0,t.jsx)(n.code,{children:"HTTP"})," port our node is using to the client app. This is just an example though, there could be many other uses for this feature."]}),"\n",(0,t.jsx)(n.p,{children:"What this looks like in the (heavily redacted) Rust code:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:'use aquadoggo::Node;\n// highlight-start\nuse tauri::{async_runtime, Manager, State};\n// highlight-end\n\nuse crate::config::{app_data_dir, load_config};\nuse crate::key_pair::generate_or_load_key_pair;\n// highlight-start\nuse crate::schema::load_schema_lock;\n// highlight-end\n\n// highlight-start\nstruct HttpPort(u16);\n\n#[tauri::command]\nfn http_port_command(state: State<HttpPort>) -> u16 {\n    state.0\n}\n// highlight-end\n\nfn setup_handler(app: &mut tauri::App) -> Result<(), Box<dyn std::error::Error + \'static>> {\n    let app = app.handle();\n    let app_data_dir = app_data_dir(&app)?;\n\n    // ...\n\n    let config = load_config(&app, &app_data_dir)?;\n    // highlight-start\n    // Add the configured nodes http port to the app state so we can access it from the frontend.\n    app.manage(HttpPort(config.http_port));\n    // highlight-end\n\n    // ...\n}\n\nfn main() {\n    // ...\n\n    tauri::Builder::default()\n        .setup(setup_handler)\n        // highlight-start\n        .invoke_handler(tauri::generate_handler![http_port_command])\n        // highlight-end\n        .run(tauri::generate_context!())\n        .expect("error while running tauri application");\n}\n\n'})}),"\n",(0,t.jsx)(n.p,{children:"And then in the frontend you'd do something like this:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-javascript",children:"// highlight-start\nimport { invoke } from '@tauri-apps/api/tauri';\n\nconst init = async () => {\n  const HTTP_PORT = await invoke('http_port_command');\n};\n// highlight-end\n"})}),"\n",(0,t.jsx)(n.h3,{id:"temp-app-storage-during-development",children:"Temp app storage during development"}),"\n",(0,t.jsx)(n.p,{children:"It was skimmed over in the code examples above, but during development we probably don't want to persist our app data between each run, so when getting the recommended app data directory path we are first checking if we are actually in dev mode, and if so creating a tmp directory which will be used for all app data instead of the recommended filesystem paths. This is done like so:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-rust",children:'// src/config.rs\n\nuse std::fs::{self, DirBuilder};\nuse std::path::PathBuf;\nuse std::sync::OnceLock;\n\nuse anyhow::Result;\nuse tauri::AppHandle;\nuse tempdir::TempDir;\n\nstatic TMP_DIR: OnceLock<TempDir> = OnceLock::new();\n\n/// Get path to the current app data directory.\n///\n/// If in dev mode app data is persisted to an ephemeral tmp folder. Otherwise app data path is\n/// based on Tauri recommended defaults and app name defined in our Tauri.conf file.\npub fn app_data_dir(app: &AppHandle) -> Result<PathBuf, anyhow::Error> {\n    let path = if cfg!(dev) {\n        TMP_DIR\n            .get_or_init(|| {\n                // Initialise a `TempDir` instance globally to make sure it does not run out of\n                // scope and gets deleted before the end of the application runtime\n                TempDir::new("p2panda-tauri-example").expect("Could not create temporary directory")\n            })\n            .path()\n            .to_path_buf()\n    } else {\n        let path = app\n            .path_resolver()\n            .app_data_dir()\n            .expect("recommended app data dir is detected");\n\n        // Create app data directory if it doesn\'t exist yet.\n        if !path.is_dir() {\n            DirBuilder::new().create(path.clone())?;\n        };\n\n        path\n    };\n\n    Ok(path)\n}\n'})}),"\n",(0,t.jsx)(n.h3,{id:"multiple-nodes--replication",children:"Multiple nodes / replication"}),"\n",(0,t.jsx)(n.p,{children:"During development it's really useful to be able to start many nodes and observe local discovery and replication of data. We built this into the p2panda Tauri boilerplate for you. To launch multiple app instances simply run the following commands:"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:"# Start a dev server for the client code which will be shared across app instances.\nnpm run dev\n\n# In a new terminal, launch an app instance\nnpm run peer\n\n# In another new terminal, launch another app instance\nnpm run peer\n\n# etc....\n"})}),"\n",(0,t.jsx)(n.p,{children:"Eventually you will end up with something which looks like this:"}),"\n",(0,t.jsx)(n.p,{children:(0,t.jsx)(n.img,{src:"https://raw.githubusercontent.com/p2panda/.github/main/assets/tauri-example-screenshot.png",alt:"tauri example screenshot with many peers"})}),"\n",(0,t.jsx)(n.admonition,{title:"Why are there different color pandas?",type:"info",children:(0,t.jsx)(n.p,{children:"Glad you asked! Panda color is deterministically generated from your client's public key ;-p this means we can see which authors published which pandas once data starts replicating around. Cool, right?!"})}),"\n",(0,t.jsx)(n.h3,{id:"multi-platform-compilation",children:"Multi-platform compilation"}),"\n",(0,t.jsx)(n.p,{children:"Last but absolutely not least, when the time comes, you will want to compile binaries for your application. And a big reason for using Tauri is the fact that it takes care of cross-compiling this very application for all major desktop operating systems (and soon mobile too)."}),"\n",(0,t.jsx)(n.h4,{id:"local-compilation",children:"Local compilation"}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:"npm run tauri build\n"})}),"\n",(0,t.jsxs)(n.p,{children:["This will bundle your app for as a ",(0,t.jsx)(n.code,{children:".deb"})," file and as an ",(0,t.jsx)(n.code,{children:"AppImage"}),"."]}),"\n",(0,t.jsxs)(n.h4,{id:"ci-bundle-release",children:[(0,t.jsx)(n.code,{children:"ci"})," bundle release"]}),"\n",(0,t.jsxs)(n.p,{children:["There is a ",(0,t.jsx)(n.code,{children:"ci"})," task, triggered on pushing a new git tag, that compiles your app for all major desktop platforms and pushes the assets to a new github release. You can trigger this with the following commands:"]}),"\n",(0,t.jsx)(n.pre,{children:(0,t.jsx)(n.code,{className:"language-bash",children:"# Create a tag for this version\ngit tag v0.1.0\n\n# Push the tag to github\ngit push origin main --tags\n"})}),"\n",(0,t.jsx)(n.p,{children:"Ok, well done making it this far, that is really the end! Happy Tauri X p2panda hacking!!"})]})}function c(e={}){const{wrapper:n}={...(0,i.a)(),...e.components};return n?(0,t.jsx)(n,{...e,children:(0,t.jsx)(h,{...e})}):h(e)}},3348:(e,n,a)=>{a.d(n,{Z:()=>t});const t=a.p+"assets/images/tauri-example-screenshot-b2b1ba7341e37b7cfd2a782efdfedbdb.png"},1151:(e,n,a)=>{a.d(n,{Z:()=>s,a:()=>r});var t=a(7294);const i={},o=t.createContext(i);function r(e){const n=t.useContext(o);return t.useMemo((function(){return"function"==typeof e?e(n):{...n,...e}}),[n,e])}function s(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:r(e.components),t.createElement(o.Provider,{value:n},e.children)}}}]);